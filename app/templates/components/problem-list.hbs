
  <div class="main-list">
    {{#if this.isSearching}}
      <p class="loading-message">Searching problems...</p>
    {{else}}
    <ul id="problem-list-ul">
      {{#each this.problems as |problem|}}
      <li>
        <ProblemListItem @problem={{problem}} @isList={{isList}} @isGrid={{isGrid}} @store={{this.store}} @moreMenuOptions={{moreMenuOptions}} @toProblemInfo={{action "toProblemInfo"}} @refreshList={{action "refreshList"}} />

      </li>
      {{/each}}
      </ul>
    {{/if}}
  </div>




